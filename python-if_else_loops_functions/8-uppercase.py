#!/usr/bin/env python3
def uppercase(str):
    if str == ord('A'):
        for i in range(ord('A') + 1):
            print(chr(i), end=' ')

        return True
        
    else:
        return KeyError

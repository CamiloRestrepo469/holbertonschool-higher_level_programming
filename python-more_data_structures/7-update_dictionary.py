#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    if value is None:
        a_dictionary[key]
    if key in a_dictionary:
        pass
    if not value:
        pass
    if not key:
        pass
    a_dictionary[key] = value
    return a_dictionary
